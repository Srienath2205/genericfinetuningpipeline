# ================================
# DATASET CONFIG (generic)
# ================================
# Expected SFT format:
# { "messages": [ {"role":"user","content":"..."}, {"role":"assistant","content":"..."} ] }

train_path: "data/train.jsonl"
eval_path: "data/eval.jsonl"
schema_path: "data/schema.json"

# Optional filtering / limits for quick tests
max_train_records: null   # e.g., 1000
max_eval_records: null    # e.g., 200

# Formatting settings
add_system_prompt: false
system_prompt: ""